package animator_model;

import animator_model.shape.AShape;

/**
 * Class represents the simple rectangle shape with scalable dimensions.
 */
public class Rectangle extends AShape {

  //TODO: build and document
  public Rectangle(int x, int y, int r, int w, int h, int g, int b) {
    super(x, y, r, w, h, g, b);
  }
}
