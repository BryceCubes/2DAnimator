package cs3500.animator.view;

public interface ReadOnlyIAnimatorView {
  /**
   * Method used to animate from a given view that takes a model as input so that the given shapes
   * can be outputted correctly.
   */
  void animate();
}
